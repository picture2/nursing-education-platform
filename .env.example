# PORT
PORT=3000

# Environment
NODE_ENV=production

# Database (optional - if you want to use MongoDB instead of JSON files)
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/nursing

# Secret Key for Sessions (generate a random string)
SECRET_KEY=your-secret-key-here-change-this

# Domain (your website URL after deployment)
DOMAIN=https://your-domain.com

# CORS Origin (same as domain)
CORS_ORIGIN=https://your-domain.com
